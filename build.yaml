# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command and `artifact-name` to assign a name to
# distinguish build outputs from each other:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#   - board: nice_nano_v2
#     shield: corne_left
#     cmake-args: -DCONFIG_ZMK_USB_LOGGING=y
#     artifact-name: corne_left_with_logging
#
# NOTE: ZMK v0.4 (Zephyr 4.1 / HWMv2) でボード名が
#       `seeeduino_xiao_ble` から `xiao_ble` へ変更され、さらに ZMK 用の
#       ボードバリアント指定 `/nrf52840/zmk` が必須になった
#       (指定しないと CI が "Missing ZMK Compat" で失敗する)。
---
include:
  - board: xiao_ble/nrf52840/zmk
    shield: mona2_r rgbled_adapter
    snippet: studio-rpc-usb-uart
    artifact-name: mona2_r-pmw3610
  - board: xiao_ble/nrf52840/zmk
    shield: mona2_r rgbled_adapter
    snippet: studio-rpc-usb-uart
    # NOTE: Zephyr は devicetree を Kconfig より先に処理するため、
    #       .overlay 内の #ifdef CONFIG_TRACKBALL_PAW3222 はクリーンビルドで効かない。
    #       センサの切り替えは paw3222.overlay 側で行う。
    cmake-args: '-DEXTRA_CONF_FILE=../../config/paw3222.conf -DEXTRA_DTC_OVERLAY_FILE=../../config/paw3222.overlay'
    artifact-name: mona2_r-paw3222
  - board: xiao_ble/nrf52840/zmk
    shield: mona2_l rgbled_adapter
  - board: xiao_ble/nrf52840/zmk
    shield: settings_reset
